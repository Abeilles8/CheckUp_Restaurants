<div>
  <h3>通知</h3>
  <%= link_to "通知を削除", notification_path(@notifications), method: :delete %>
    <% if @notifications.exists? %>
      <div>
        <%= render @notifications %>
      </div>
    <% else %>
      <p>通知はありません</p>
    <% end %>
</div>